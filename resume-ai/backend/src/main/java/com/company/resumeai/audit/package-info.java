/**
 * Generation audit trail (§32): retrieved fragment IDs, prompt version, model
 * name, validation scores, rewrite attempts, user edits, approval status.
 * Reserved alongside Milestone 5.
 */
package com.company.resumeai.audit;
