/**
 * Spring {@code @Configuration} beans (e.g. embedding client, LLM client wiring)
 * once those providers exist. Empty for Milestone 1 - application.yml covers
 * current configuration needs.
 */
package com.company.resumeai.config;
